{
  "name": "bskybots",
  "version": "1.0.0",
  "description": "A Bluesky bot that posts mood messages and responds to mentions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["bluesky", "bot", "typescript"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@atproto/api": "^0.3.7",
    "cron": "^2.3.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}